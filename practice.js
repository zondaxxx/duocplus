window.PRACTICE_CPP = {"d1l1": {"goal": "Напиши свою первую программу на C++. Программа должна вывести в консоль ровно строку <code>Hello, C++!</code> (с восклицательным знаком). Это базовый шаг: написать <code>main</code>, собрать и запустить.", "starter": "#include <iostream>\n\nint main() {\n    // TODO: вывести строку Hello, C++!\n    return 0;\n}", "stdin": "", "expect": "Hello, C++!", "hints": ["Для вывода в консоль есть стандартный поток вывода — <code>std::cout</code>.", "Используй <code>std::cout &lt;&lt; ... ;</code>, а в конце можешь добавить <code>std::endl</code> или <code>\"\\n\"</code> для перевода строки.", "Строка целиком: <code>std::cout &lt;&lt; \"Hello, C++!\" &lt;&lt; std::endl;</code>"], "solution": "#include <iostream>\n\nint main() {\n    std::cout << \"Hello, C++!\" << std::endl;\n    return 0;\n}"}, "d1l5": {"goal": "Создай класс <code>Account</code> с <b>private</b>-полем <code>balance</code> и свободной <b>friend</b>-функцией <code>printBalance</code>, которая печатает это поле. Создай объект с балансом <code>100</code> и выведи его через friend-функцию. Ожидаемый вывод: <code>100</code>.", "starter": "#include <iostream>\n\nclass Account {\nprivate:\n    int balance;\npublic:\n    Account(int b) : balance(b) {}\n    // TODO: объявить friend-функцию printBalance\n};\n\n// TODO: определить printBalance, печатающую balance\n\nint main() {\n    Account acc(100);\n    // TODO: вызвать printBalance для acc\n    return 0;\n}", "stdin": "", "expect": "100", "hints": ["friend-функция объявляется внутри класса, но не является его методом — зато имеет доступ к private-полям.", "Внутри класса напиши <code>friend void printBalance(const Account&amp; a);</code>, а саму функцию определи снаружи.", "Тело функции: <code>std::cout &lt;&lt; a.balance &lt;&lt; std::endl;</code> — и вызови <code>printBalance(acc);</code> в <code>main</code>."], "solution": "#include <iostream>\n\nclass Account {\nprivate:\n    int balance;\npublic:\n    Account(int b) : balance(b) {}\n    friend void printBalance(const Account& a);\n};\n\nvoid printBalance(const Account& a) {\n    std::cout << a.balance << std::endl;\n}\n\nint main() {\n    Account acc(100);\n    printBalance(acc);\n    return 0;\n}"}, "d2l1": {"goal": "Создай класс <code>Point</code> с двумя полями <code>x</code> и <code>y</code> типа <code>int</code>. Напиши конструктор, который через <b>список инициализации</b> (<code>: x(x), y(y)</code>) задаёт поля. В <code>main</code> создай объект <code>Point p(3, 4)</code> и выведи его поля через пробел. Ожидаемый вывод: <code>3 4</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Point {\n    int x;\n    int y;\npublic:\n    // TODO: конструктор со списком инициализации : x(x), y(y)\n\n    // TODO: метод print(), который выводит \"x y\"\n};\n\nint main() {\n    // TODO: создай Point p(3, 4) и вызови p.print();\n    return 0;\n}\n", "stdin": "", "expect": "3 4", "hints": ["Конструктор принимает два параметра и присваивает их полям через двоеточие ПЕРЕД телом конструктора — это и есть список инициализации.", "Объяви метод так: <code>Point(int x, int y) : x(x), y(y) {}</code>. Для вывода используй <code>cout << x << \" \" << y << endl;</code>.", "Тело main: <code>Point p(3, 4); p.print();</code>"], "solution": "#include <iostream>\nusing namespace std;\n\nclass Point {\n    int x;\n    int y;\npublic:\n    Point(int x, int y) : x(x), y(y) {}\n    void print() const { cout << x << \" \" << y << endl; }\n};\n\nint main() {\n    Point p(3, 4);\n    p.print();\n    return 0;\n}\n"}, "d2l2": {"goal": "Создай класс <code>Meter</code> с полем <code>value</code> и <b>explicit</b>-конструктором от одного <code>int</code>. Ключевое слово <code>explicit</code> запрещает неявное преобразование (<code>Meter m = 5;</code> не скомпилируется — нужно <code>Meter m(5)</code>). Создай объект <code>Meter m(5)</code> и выведи <code>m.get()</code>. Ожидаемый вывод: <code>5</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Meter {\n    int value;\npublic:\n    // TODO: explicit-конструктор от int\n\n    // TODO: метод get(), возвращающий value\n};\n\nint main() {\n    // TODO: создай Meter m(5) и выведи m.get()\n    return 0;\n}\n", "stdin": "", "expect": "5", "hints": ["Перед конструктором с одним параметром поставь ключевое слово <code>explicit</code> — теперь объект создаётся только явно, через круглые скобки.", "Напиши <code>explicit Meter(int v) : value(v) {}</code> и геттер <code>int get() const { return value; }</code>.", "В main: <code>Meter m(5); cout << m.get() << endl;</code>"], "solution": "#include <iostream>\nusing namespace std;\n\nclass Meter {\n    int value;\npublic:\n    explicit Meter(int v) : value(v) {}\n    int get() const { return value; }\n};\n\nint main() {\n    Meter m(5);\n    cout << m.get() << endl;\n    return 0;\n}\n"}, "d2l3": {"goal": "Создай класс <code>Widget</code> со <b>статическим</b> полем-счётчиком <code>count</code>, которое увеличивается в конструкторе при каждом создании объекта. Создай <b>три</b> объекта и выведи <code>Widget::count</code>. Не забудь определить статическое поле вне класса. Ожидаемый вывод: <code>3</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Widget {\npublic:\n    static int count;\n    // TODO: конструктор, увеличивающий count\n};\n\n// TODO: определи Widget::count = 0 вне класса\n\nint main() {\n    // TODO: создай 3 объекта Widget и выведи Widget::count\n    return 0;\n}\n", "stdin": "", "expect": "3", "hints": ["Статическое поле одно на весь класс. Его нужно объявить внутри класса, а определить (инициализировать) ОТДЕЛЬНО вне класса.", "Внутри класса: <code>Widget() { ++count; }</code>. Вне класса обязательно: <code>int Widget::count = 0;</code>.", "В main создай <code>Widget a; Widget b; Widget c;</code> и выведи <code>cout << Widget::count << endl;</code>"], "solution": "#include <iostream>\nusing namespace std;\n\nclass Widget {\npublic:\n    static int count;\n    Widget() { ++count; }\n};\n\nint Widget::count = 0;\n\nint main() {\n    Widget a;\n    Widget b;\n    Widget c;\n    cout << Widget::count << endl;\n    return 0;\n}\n"}, "d2l4": {"goal": "Создай класс <code>Vec</code> с полями <code>x</code>, <code>y</code> и перегрузи <b>operator+</b>, чтобы он возвращал новый <code>Vec</code> с покомпонентной суммой. Сложи <code>Vec(1, 2)</code> и <code>Vec(3, 4)</code> и выведи поля результата через пробел. Ожидаемый вывод: <code>4 6</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Vec {\npublic:\n    int x, y;\n    Vec(int x, int y) : x(x), y(y) {}\n    // TODO: перегрузи operator+ (возвращает новый Vec)\n};\n\nint main() {\n    Vec a(1, 2);\n    Vec b(3, 4);\n    // TODO: c = a + b; вывести c.x и c.y\n    return 0;\n}\n", "stdin": "", "expect": "4 6", "hints": ["operator+ — обычный метод, который принимает другой объект и возвращает НОВЫЙ объект-сумму, не меняя исходные.", "Сигнатура: <code>Vec operator+(const Vec& other) const { return Vec(x + other.x, y + other.y); }</code>.", "В main: <code>Vec c = a + b; cout << c.x << \" \" << c.y << endl;</code>"], "solution": "#include <iostream>\nusing namespace std;\n\nclass Vec {\npublic:\n    int x, y;\n    Vec(int x, int y) : x(x), y(y) {}\n    Vec operator+(const Vec& other) const {\n        return Vec(x + other.x, y + other.y);\n    }\n};\n\nint main() {\n    Vec a(1, 2);\n    Vec b(3, 4);\n    Vec c = a + b;\n    cout << c.x << \" \" << c.y << endl;\n    return 0;\n}\n"}, "d2l5": {"goal": "Создай класс <code>Counter</code> с полем <code>value</code> и перегрузи <b>префиксный</b> <code>operator++</code> (увеличивает <code>value</code> на 1 и возвращает ссылку на сам объект). Создай <code>Counter c(10)</code>, примени <code>++c</code> дважды и выведи <code>c.value</code>. Ожидаемый вывод: <code>12</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Counter {\npublic:\n    int value;\n    Counter(int v) : value(v) {}\n    // TODO: префиксный operator++ (возвращает Counter&)\n};\n\nint main() {\n    Counter c(10);\n    // TODO: примени ++c дважды и выведи c.value\n    return 0;\n}\n", "stdin": "", "expect": "12", "hints": ["Префиксный operator++ не имеет параметров (в отличие от постфиксного с фиктивным int). Он меняет объект и возвращает ссылку на него.", "Напиши: <code>Counter& operator++() { ++value; return *this; }</code>.", "В main: <code>++c; ++c; cout << c.value << endl;</code> — начав с 10, получим 12."], "solution": "#include <iostream>\nusing namespace std;\n\nclass Counter {\npublic:\n    int value;\n    Counter(int v) : value(v) {}\n    Counter& operator++() {\n        ++value;\n        return *this;\n    }\n};\n\nint main() {\n    Counter c(10);\n    ++c;\n    ++c;\n    cout << c.value << endl;\n    return 0;\n}\n"}, "d3l2": {"goal": "<b>Открытое (public) наследование.</b> Создай базовый класс <code>Base</code> с методом <code>hello()</code>, который выводит <code>Hello from Base</code>. Класс <code>Derived</code> наследует от <code>Base</code> через <code>public</code>. В <code>main()</code> создай объект <code>Derived</code> и вызови у него унаследованный метод. Программа должна вывести: <code>Hello from Base</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    void hello() { cout << \"Hello from Base\" << endl; }\n};\n\n// TODO: объяви класс Derived, наследующий Base через public\n\nint main() {\n    // TODO: создай объект Derived и вызови у него hello()\n    return 0;\n}", "stdin": "", "expect": "Hello from Base", "hints": ["При public-наследовании объект Derived получает все public-методы Base и может вызывать их напрямую, как свои собственные.", "Объяви класс так: class Derived : public Base { }; — тело может быть пустым. Затем создай объект Derived и вызови у него .hello().", "Внутри main: Derived d; d.hello(); — метод унаследован, отдельно его описывать не нужно."], "solution": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    void hello() { cout << \"Hello from Base\" << endl; }\n};\n\nclass Derived : public Base {\n};\n\nint main() {\n    Derived d;\n    d.hello();\n    return 0;\n}"}, "d3l3": {"goal": "<b>Конструкторы при наследовании.</b> У <code>Base</code> есть конструктор <code>Base(int x)</code>, печатающий <code>Base &lt;x&gt;</code>. Класс <code>Derived</code> наследует <code>Base</code> и в своём конструкторе через список инициализации вызывает <code>Base(10)</code>, после чего печатает <code>Derived</code>. Создай объект <code>Derived</code>. Сначала отрабатывает конструктор базового класса, потом производного, поэтому вывод должен быть:<br><code>Base 10</code><br><code>Derived</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    Base(int x) {\n        cout << \"Base \" << x << endl;\n    }\n};\n\nclass Derived : public Base {\npublic:\n    // TODO: конструктор Derived должен вызвать Base(10) и вывести \"Derived\"\n};\n\nint main() {\n    // TODO: создай объект Derived\n    return 0;\n}", "stdin": "", "expect": "Base 10\nDerived", "hints": ["Конструктор базового класса всегда выполняется ПЕРЕД телом конструктора производного. Base(int) не имеет конструктора по умолчанию, поэтому Derived обязан явно передать ему аргумент.", "Вызови базовый конструктор через список инициализации после двоеточия: Derived() : Base(10) { ... }. Внутри тела выведи \"Derived\".", "Напиши: Derived() : Base(10) { cout << \"Derived\" << endl; } а в main создай Derived d;"], "solution": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    Base(int x) {\n        cout << \"Base \" << x << endl;\n    }\n};\n\nclass Derived : public Base {\npublic:\n    Derived() : Base(10) {\n        cout << \"Derived\" << endl;\n    }\n};\n\nint main() {\n    Derived d;\n    return 0;\n}"}, "d3l4": {"goal": "<b>Ромбовидное (виртуальное) наследование.</b> Есть класс <code>A</code> с полем <code>value = 42</code>. Классы <code>B</code> и <code>C</code> наследуют <code>A</code> через <code>public virtual</code>. Класс <code>D</code> наследует и <code>B</code>, и <code>C</code>. Благодаря виртуальному наследованию подобъект <code>A</code> в <code>D</code> единственный, поэтому к <code>value</code> можно обратиться без неоднозначности. Создай объект <code>D</code> и выведи его <code>value</code>. Ожидается: <code>42</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass A {\npublic:\n    int value = 42;\n};\n\n// TODO: B и C наследуют A через public virtual\n// TODO: D наследует B и C\n\nint main() {\n    // TODO: создай объект D и выведи d.value\n    return 0;\n}", "stdin": "", "expect": "42", "hints": ["Без слова virtual объект D содержал бы ДВЕ копии A (через B и через C), и обращение к value было бы неоднозначным. virtual делает подобъект A общим — копия одна.", "Объяви: class B : public virtual A {}; и class C : public virtual A {}; затем class D : public B, public C {};", "В main: D d; cout << d.value << endl; — поле value доступно напрямую, потому что A унаследован виртуально."], "solution": "#include <iostream>\nusing namespace std;\n\nclass A {\npublic:\n    int value = 42;\n};\n\nclass B : public virtual A {};\nclass C : public virtual A {};\n\nclass D : public B, public C {};\n\nint main() {\n    D d;\n    cout << d.value << endl;\n    return 0;\n}"}, "d4l1": {"goal": "<b>Полиморфизм.</b> Объяви класс <code>Base</code> с <code>virtual</code> методом <code>print()</code> и наследника <code>Derived</code>, который переопределяет (<code>override</code>) этот метод. Через указатель <code>Base*</code>, указывающий на объект <code>Derived</code>, вызови <code>print()</code>. Должна вывестись версия наследника: <code>Derived v2</code>.", "starter": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    // TODO: virtual void print() const ...  (вывести \"Base v1\")\n    virtual ~Base() = default;\n};\n\nclass Derived : public Base {\npublic:\n    // TODO: override print() -> вывести \"Derived v2\"\n};\n\nint main() {\n    Base* p = new Derived();\n    // TODO: вызови p->print();\n    delete p;\n    return 0;\n}", "stdin": "", "expect": "Derived v2", "hints": ["Указатель типа Base*, но объект на самом деле Derived. Раз метод virtual, выбор версии произойдёт по реальному типу объекта во время выполнения.", "В Base метод помечен virtual, в Derived — тем же сигнатурой и словом override. Вызов p->print() сам выберет нужную версию.", "В Derived: void print() const override { cout << \"Derived v2\" << endl; } — и в main просто p->print();"], "solution": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    virtual void print() const { cout << \"Base v1\" << endl; }\n    virtual ~Base() = default;\n};\n\nclass Derived : public Base {\npublic:\n    void print() const override { cout << \"Derived v2\" << endl; }\n};\n\nint main() {\n    Base* p = new Derived();\n    p->print();\n    delete p;\n    return 0;\n}"}, "d4l2": {"goal": "<b>Виртуальный деструктор.</b> Сделай деструктор <code>Base</code> виртуальным (печатает <code>~Base</code>), а в <code>Derived</code> деструктор печатает <code>~Derived</code>. Создай объект через <code>Base* p = new Derived;</code> и вызови <code>delete p;</code>. Благодаря virtual-деструктору сработают оба деструктора в правильном порядке:<br><code>~Derived</code><br><code>~Base</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    // TODO: virtual ~Base() { ... вывести \"~Base\" }\n};\n\nclass Derived : public Base {\npublic:\n    // TODO: ~Derived() override { ... вывести \"~Derived\" }\n};\n\nint main() {\n    Base* p = new Derived();\n    // TODO: delete p;\n    return 0;\n}", "stdin": "", "expect": "~Derived\n~Base", "hints": ["При delete через Base* нужно, чтобы вызвался деструктор реального объекта (Derived). Для этого деструктор базы должен быть virtual.", "Сначала отрабатывает деструктор наследника, затем базового класса — поэтому ~Derived печатается раньше ~Base.", "virtual ~Base() { cout << \"~Base\" << endl; } и ~Derived() override { cout << \"~Derived\" << endl; }, в main — delete p;"], "solution": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    virtual ~Base() { cout << \"~Base\" << endl; }\n};\n\nclass Derived : public Base {\npublic:\n    ~Derived() override { cout << \"~Derived\" << endl; }\n};\n\nint main() {\n    Base* p = new Derived();\n    delete p;\n    return 0;\n}"}, "d4l3": {"goal": "<b>Абстрактный класс.</b> Объяви абстрактный класс <code>Shape</code> с чисто виртуальными (<code>= 0</code>) методами <code>area()</code> и <code>print()</code>. Сделай наследника <code>Rectangle</code> со сторонами 3 и 4: <code>area()</code> возвращает площадь, <code>print()</code> выводит её. Вызови полиморфно через <code>Shape*</code>. Ожидается:<br><code>Rectangle area=12</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Shape {\npublic:\n    // TODO: чисто виртуальные area() и print() ( = 0 )\n    virtual ~Shape() = default;\n};\n\nclass Rectangle : public Shape {\n    double w, h;\npublic:\n    Rectangle(double w, double h) : w(w), h(h) {}\n    // TODO: override area() -> w*h\n    // TODO: override print() -> вывести \"Rectangle area=\" и площадь\n};\n\nint main() {\n    Shape* s = new Rectangle(3, 4);\n    // TODO: s->print();\n    delete s;\n    return 0;\n}", "stdin": "", "expect": "Rectangle area=12", "hints": ["Чисто виртуальный метод объявляется с = 0 и не имеет тела в базовом классе — поэтому Shape нельзя создать напрямую, только наследника.", "В Rectangle переопредели оба метода с override. print() может вызвать area() внутри себя.", "double area() const override { return w*h; } и void print() const override { cout << \"Rectangle area=\" << area() << endl; } — 3*4 даёт 12."], "solution": "#include <iostream>\nusing namespace std;\n\nclass Shape {\npublic:\n    virtual double area() const = 0;\n    virtual void print() const = 0;\n    virtual ~Shape() = default;\n};\n\nclass Rectangle : public Shape {\n    double w, h;\npublic:\n    Rectangle(double w, double h) : w(w), h(h) {}\n    double area() const override { return w * h; }\n    void print() const override { cout << \"Rectangle area=\" << area() << endl; }\n};\n\nint main() {\n    Shape* s = new Rectangle(3, 4);\n    s->print();\n    delete s;\n    return 0;\n}"}, "d4l4": {"goal": "<b>Свойство виртуальных функций: параметр по умолчанию.</b> В <code>Base</code> сделай <code>virtual void show(int x = 10)</code> (печатает <code>Base x</code>), в <code>Derived</code> переопредели <code>show(int x = 20)</code> (печатает <code>Derived x</code>). Вызови <code>p->show()</code> через <code>Base* p = new Derived;</code>. Хитрость: <b>тело</b> берётся из Derived, а <b>значение по умолчанию</b> — из статического типа Base. Поэтому вывод:<br><code>Derived 10</code>", "starter": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    // TODO: virtual void show(int x = 10) const -> вывести \"Base \" << x\n    virtual ~Base() = default;\n};\n\nclass Derived : public Base {\npublic:\n    // TODO: override void show(int x = 20) const -> вывести \"Derived \" << x\n};\n\nint main() {\n    Base* p = new Derived();\n    // TODO: вызови p->show();  (без аргумента)\n    delete p;\n    return 0;\n}", "stdin": "", "expect": "Derived 10", "hints": ["Какой метод выполнится — решается во время выполнения по реальному типу (Derived). А вот значение аргумента по умолчанию подставляется на этапе компиляции.", "Компилятор смотрит на тип указателя (Base*) и берёт default-значение оттуда — то есть 10, хотя тело выполнится из Derived.", "show(int x = 10) в Base и show(int x = 20) override в Derived; вызов p->show() даст тело Derived со значением 10 -> \"Derived 10\"."], "solution": "#include <iostream>\nusing namespace std;\n\nclass Base {\npublic:\n    virtual void show(int x = 10) const { cout << \"Base \" << x << endl; }\n    virtual ~Base() = default;\n};\n\nclass Derived : public Base {\npublic:\n    void show(int x = 20) const override { cout << \"Derived \" << x << endl; }\n};\n\nint main() {\n    Base* p = new Derived();\n    p->show();\n    delete p;\n    return 0;\n}"}, "d5l2": {"goal": "Напиши <b>шаблон функции</b> <code>myMax</code>, который возвращает большее из двух значений. Вызови его для <code>int</code> (<code>3</code> и <code>7</code>) и для <code>double</code> (<code>2.5</code> и <code>1.5</code>), выведи каждый результат на отдельной строке. Ожидаемый вывод:<br><code>7</code><br><code>2.5</code>", "starter": "#include <iostream>\nusing namespace std;\n\n// TODO: напиши шаблон функции template<typename T> T myMax(T a, T b)\n// который возвращает большее из a и b\n\nint main() {\n    // TODO: вызови myMax(3, 7) и myMax(2.5, 1.5), выведи каждый с endl\n    return 0;\n}", "stdin": "", "expect": "7\n2.5", "hints": ["Один шаблон работает и для int, и для double — тип подставится сам при вызове.", "Объяви template <typename T> перед функцией: T myMax(T a, T b) { ... }. Внутри верни большее через тернарный оператор (a > b) ? a : b.", "return (a > b) ? a : b; а в main: cout << myMax(3, 7) << endl; cout << myMax(2.5, 1.5) << endl;"], "solution": "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT myMax(T a, T b) {\n    return (a > b) ? a : b;\n}\n\nint main() {\n    cout << myMax(3, 7) << endl;\n    cout << myMax(2.5, 1.5) << endl;\n    return 0;\n}"}, "d5l3": {"goal": "Сделай <b>общий шаблон</b> функции <code>typeName(T)</code>, который возвращает строку <code>\"generic\"</code>, и его <b>специализацию для <code>int</code></b>, возвращающую <code>\"int\"</code>. Вызови функцию для <code>double</code> (<code>3.14</code>) и для <code>int</code> (<code>42</code>), выведи результаты. Ожидаемый вывод:<br><code>generic</code><br><code>int</code>", "starter": "#include <iostream>\n#include <string>\nusing namespace std;\n\n// TODO: общий шаблон template<typename T> string typeName(T) -> \"generic\"\n\n// TODO: полная специализация template<> string typeName(int) -> \"int\"\n\nint main() {\n    // TODO: выведи typeName(3.14) и typeName(42), каждый с endl\n    return 0;\n}", "stdin": "", "expect": "generic\nint", "hints": ["Нужны две версии одной функции: общая (для любого типа) и отдельная — только для int.", "Общий шаблон: template <typename T> string typeName(T) { return \"generic\"; }. Специализация пишется с template <> и конкретным типом: template <> string typeName(int).", "template <> string typeName(int) { return \"int\"; } а в main: cout << typeName(3.14) << endl; cout << typeName(42) << endl;"], "solution": "#include <iostream>\n#include <string>\nusing namespace std;\n\ntemplate <typename T>\nstring typeName(T) { return \"generic\"; }\n\ntemplate <>\nstring typeName(int) { return \"int\"; }\n\nint main() {\n    cout << typeName(3.14) << endl;\n    cout << typeName(42) << endl;\n    return 0;\n}"}, "d5l4": {"goal": "Объяви <b>шаблон класса</b> <code>Box&lt;T&gt;</code> с методом <code>what()</code>, возвращающим <code>\"value\"</code>, и его <b>частичную специализацию</b> <code>Box&lt;T*&gt;</code> для указателей, возвращающую <code>\"pointer\"</code>. Создай <code>Box&lt;int&gt;</code> и <code>Box&lt;int*&gt;</code>, выведи результат <code>what()</code> у каждого. Ожидаемый вывод:<br><code>value</code><br><code>pointer</code>", "starter": "#include <iostream>\n#include <string>\nusing namespace std;\n\n// TODO: основной шаблон template<typename T> struct Box { string what() -> \"value\" };\n\n// TODO: частичная специализация template<typename T> struct Box<T*> { what() -> \"pointer\" };\n\nint main() {\n    // TODO: создай Box<int> и Box<int*>, выведи what() у каждого с endl\n    return 0;\n}", "stdin": "", "expect": "value\npointer", "hints": ["Нужны два варианта класса: общий Box<T> и отдельный — для случая, когда T это указатель (T*).", "Основной шаблон: template <typename T> struct Box { string what() { return \"value\"; } };. Частичная специализация объявляется так: template <typename T> struct Box<T*> { ... };.", "template <typename T> struct Box<T*> { string what() { return \"pointer\"; } }; а в main: Box<int> a; Box<int*> b; cout << a.what() << endl; cout << b.what() << endl;"], "solution": "#include <iostream>\n#include <string>\nusing namespace std;\n\ntemplate <typename T>\nstruct Box {\n    string what() { return \"value\"; }\n};\n\ntemplate <typename T>\nstruct Box<T*> {\n    string what() { return \"pointer\"; }\n};\n\nint main() {\n    Box<int> a;\n    Box<int*> b;\n    cout << a.what() << endl;\n    cout << b.what() << endl;\n    return 0;\n}"}, "d6l1": {"goal": "Вычислите факториал <b>на этапе компиляции</b> с помощью рекурсивного шаблона <code>Factorial&lt;N&gt;</code>. Программа должна вывести значение <code>Factorial&lt;5&gt;::value</code>, то есть <b>120</b>.", "starter": "#include <iostream>\n\n// TODO: общий шаблон Factorial<N>: value = N * Factorial<N-1>::value\n\n// TODO: специализация Factorial<0>: value = 1\n\nint main() {\n    // TODO: вывести Factorial<5>::value\n    return 0;\n}", "stdin": "", "expect": "120", "hints": ["Факториал считается рекурсивно: 5! = 5 * 4!, а база рекурсии — 0! = 1. В шаблонах рекурсия раскрывается компилятором.", "Сделайте шаблон template<int N> struct Factorial с полем static const int value = N * Factorial<N-1>::value, и отдельную специализацию template<> struct Factorial<0> со value = 1.", "static const int value = N * Factorial<N - 1>::value; а для Factorial<0> — static const int value = 1; затем std::cout << Factorial<5>::value;"], "solution": "#include <iostream>\n\ntemplate <int N>\nstruct Factorial {\n    static const int value = N * Factorial<N - 1>::value;\n};\n\ntemplate <>\nstruct Factorial<0> {\n    static const int value = 1;\n};\n\nint main() {\n    std::cout << Factorial<5>::value << std::endl;\n    return 0;\n}"}, "d6l3": {"goal": "Напишите <b>variadic-шаблон</b> <code>sum(...)</code>, складывающий любое число аргументов через свёртку (fold expression). Вызовите <code>sum(1, 2, 3, 4, 5)</code> и выведите результат — <b>15</b>.", "starter": "#include <iostream>\n\n// TODO: variadic-шаблон sum, возвращающий сумму всех аргументов через свёртку\n\nint main() {\n    // TODO: вывести sum(1, 2, 3, 4, 5)\n    return 0;\n}", "stdin": "", "expect": "15", "hints": ["Нужен шаблон с переменным числом параметров (template<typename... Args>) и свёртка по операции +.", "Объявите параметр-пакет Args... args и верните выражение свёртки (args + ...).", "return (args + ...); внутри template<typename... Args> auto sum(Args... args), затем std::cout << sum(1, 2, 3, 4, 5);"], "solution": "#include <iostream>\n\ntemplate <typename... Args>\nauto sum(Args... args) {\n    return (args + ...);\n}\n\nint main() {\n    std::cout << sum(1, 2, 3, 4, 5) << std::endl;\n    return 0;\n}"}, "d6l4": {"goal": "Реализуйте функцию <code>divide(a, b)</code>, которая при <code>b == 0</code> бросает <code>std::runtime_error(\"division by zero\")</code>. В <code>main</code> вызовите <code>divide(10, 0)</code> в блоке <code>try</code> и в <code>catch</code> выведите текст ошибки. Ожидается строка <b>division by zero</b>.", "starter": "#include <iostream>\n#include <stdexcept>\n\n// TODO: int divide(int a, int b): если b == 0 — throw std::runtime_error(\"division by zero\")\n\nint main() {\n    // TODO: вызвать divide(10, 0) в try, поймать исключение в catch и вывести e.what()\n    return 0;\n}", "stdin": "", "expect": "division by zero", "hints": ["Проверьте делитель внутри divide и при нуле бросьте исключение оператором throw.", "Бросьте throw std::runtime_error(\"division by zero\"); а в main оберните вызов в try { ... } catch (const std::runtime_error& e) { ... }.", "В catch напишите std::cout << e.what() << std::endl; — метод what() вернёт текст \"division by zero\"."], "solution": "#include <iostream>\n#include <stdexcept>\n\nint divide(int a, int b) {\n    if (b == 0) {\n        throw std::runtime_error(\"division by zero\");\n    }\n    return a / b;\n}\n\nint main() {\n    try {\n        std::cout << divide(10, 0) << std::endl;\n    } catch (const std::runtime_error& e) {\n        std::cout << e.what() << std::endl;\n    }\n    return 0;\n}"}, "d6l5": {"goal": "Создайте <code>std::unique_ptr&lt;int&gt;</code> со значением <b>42</b> через <code>std::make_unique</code> и выведите значение, на которое он указывает (разыменование <code>*p</code>). Ожидается <b>42</b>.", "starter": "#include <iostream>\n#include <memory>\n\nint main() {\n    // TODO: создать unique_ptr<int> со значением 42 через std::make_unique\n    // TODO: вывести значение по указателю (*p)\n    return 0;\n}", "stdin": "", "expect": "42", "hints": ["Умный указатель unique_ptr сам владеет объектом; создавать его удобнее через make_unique, а значение получать разыменованием.", "Напишите auto p = std::make_unique<int>(42); — это выделит int со значением 42.", "Выведите значение через разыменование: std::cout << *p << std::endl;"], "solution": "#include <iostream>\n#include <memory>\n\nint main() {\n    std::unique_ptr<int> p = std::make_unique<int>(42);\n    std::cout << *p << std::endl;\n    return 0;\n}"}, "d7l1": {"goal": "Создай <code>std::vector&lt;int&gt;</code> и добавь в него через <code>push_back</code> числа <b>10, 20, 30</b>. Выведи в одной строке через пробел: <b>размер вектора</b> (<code>size()</code>) и <b>сумму элементов</b>. Ожидается: <code>3 60</code>.", "starter": "#include <iostream>\n#include <vector>\nint main() {\n    std::vector<int> v;\n    // TODO: push_back чисел 10, 20, 30\n    // TODO: посчитать сумму элементов\n    // TODO: вывести size() и сумму через пробел\n    return 0;\n}", "stdin": "", "expect": "3 60", "hints": ["Добавь три числа методом push_back, затем пройдись циклом по вектору и накопи сумму.", "Размер берётся через v.size(); сумму удобно считать в range-based for: for (int x : v) sum += x;", "Вывод: std::cout << v.size() << \" \" << sum << std::endl;"], "solution": "#include <iostream>\n#include <vector>\nint main() {\n    std::vector<int> v;\n    v.push_back(10);\n    v.push_back(20);\n    v.push_back(30);\n    int sum = 0;\n    for (int x : v) sum += x;\n    std::cout << v.size() << \" \" << sum << std::endl;\n    return 0;\n}"}, "d7l3": {"goal": "Создай <code>std::stack&lt;int&gt;</code> и положи в него (<code>push</code>) числа <b>1, 2, 3</b>. Выведи <b>верхний элемент</b> (<code>top()</code>), затем сделай один <code>pop()</code> и выведи <b>новый верхний</b>. Числа через пробел. Стек работает по принципу LIFO. Ожидается: <code>3 2</code>.", "starter": "#include <iostream>\n#include <stack>\nint main() {\n    std::stack<int> s;\n    // TODO: push 1, 2, 3\n    // TODO: вывести top()\n    // TODO: pop() и вывести новый top()\n    return 0;\n}", "stdin": "", "expect": "3 2", "hints": ["LIFO: последним положили 3 — значит сверху именно 3. После pop() сверху окажется предыдущий элемент.", "Используй s.push(...), s.top() для чтения верхушки и s.pop() чтобы убрать её.", "std::cout << s.top(); s.pop(); std::cout << \" \" << s.top() << std::endl;"], "solution": "#include <iostream>\n#include <stack>\nint main() {\n    std::stack<int> s;\n    s.push(1);\n    s.push(2);\n    s.push(3);\n    std::cout << s.top();\n    s.pop();\n    std::cout << \" \" << s.top() << std::endl;\n    return 0;\n}"}, "d7l4": {"goal": "Создай <code>std::map&lt;std::string, int&gt;</code> и вставь пары: <code>\"Tom\"=25</code>, <code>\"Ann\"=30</code>, <code>\"Bob\"=19</code>. Выведи <b>значение по ключу</b> <code>\"Ann\"</code>. Ожидается: <code>30</code>.", "starter": "#include <iostream>\n#include <map>\n#include <string>\nint main() {\n    std::map<std::string, int> ages;\n    // TODO: вставить пары Tom=25, Ann=30, Bob=19\n    // TODO: вывести значение по ключу \"Ann\"\n    return 0;\n}", "stdin": "", "expect": "30", "hints": ["В map значения удобно задавать через оператор [] по ключу-строке.", "Вставка: ages[\"Tom\"] = 25; и так далее. Доступ к значению — тоже через ages[\"Ann\"].", "std::cout << ages[\"Ann\"] << std::endl;"], "solution": "#include <iostream>\n#include <map>\n#include <string>\nint main() {\n    std::map<std::string, int> ages;\n    ages[\"Tom\"] = 25;\n    ages[\"Ann\"] = 30;\n    ages[\"Bob\"] = 19;\n    std::cout << ages[\"Ann\"] << std::endl;\n    return 0;\n}"}, "d8l1": {"goal": "<b>Приведение типов:</b> объяви <code>double price = 7.89;</code>. С помощью <code>static_cast&lt;int&gt;</code> преобразуй его в <code>int</code> (дробная часть отбрасывается, без округления) и выведи результат. Ожидаемый вывод: <code>7</code>.", "starter": "#include <iostream>\nusing namespace std;\n\nint main() {\n    double price = 7.89;\n    // TODO: преобразуй price к int через static_cast и выведи результат\n    return 0;\n}", "stdin": "", "expect": "7", "hints": ["static_cast при приведении double к int просто отбрасывает дробную часть (усечение), а не округляет. Значит 7.89 станет 7.", "Синтаксис: int whole = static_cast<int>(price);", "Напиши int whole = static_cast<int>(price); затем cout << whole << endl;"], "solution": "#include <iostream>\nusing namespace std;\n\nint main() {\n    double price = 7.89;\n    int whole = static_cast<int>(price);\n    cout << whole << endl;\n    return 0;\n}"}, "d8l4": {"goal": "<b>Алгоритмы STL:</b> дан вектор <code>{10, 20, 30, 40}</code>. С помощью <code>std::accumulate</code> из <code>&lt;numeric&gt;</code> посчитай сумму всех элементов и выведи её. Ожидаемый вывод: <code>100</code>.", "starter": "#include <iostream>\n#include <vector>\n#include <numeric>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {10, 20, 30, 40};\n    // TODO: посчитай сумму через accumulate и выведи её\n    return 0;\n}", "stdin": "", "expect": "100", "hints": ["accumulate проходит по диапазону и складывает элементы, начиная с заданного начального значения.", "Синтаксис: accumulate(nums.begin(), nums.end(), 0) — третий аргумент это начальное значение суммы.", "Напиши int total = accumulate(nums.begin(), nums.end(), 0); затем cout << total << endl;"], "solution": "#include <iostream>\n#include <vector>\n#include <numeric>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {10, 20, 30, 40};\n    int total = accumulate(nums.begin(), nums.end(), 0);\n    cout << total << endl;\n    return 0;\n}"}, "d8l5": {"goal": "<b>Лямбды и захват:</b> есть <code>int base = 100;</code> и <code>int bonus = 25;</code>. Создай лямбду, которая захватывает <code>base</code> <b>по значению</b>, а <code>bonus</code> <b>по ссылке</b>, и возвращает их сумму. После создания лямбды поменяй <code>bonus = 50;</code>, затем вызови лямбду и выведи результат. Ожидаемый вывод: <code>150</code>.", "starter": "#include <iostream>\nusing namespace std;\n\nint main() {\n    int base = 100;\n    int bonus = 25;\n    // TODO: лямбда [base, &bonus]() -> base + bonus\n    bonus = 50;\n    // TODO: вызови лямбду и выведи результат\n    return 0;\n}", "stdin": "", "expect": "150", "hints": ["Захват по значению фиксирует копию переменной в момент создания лямбды, а захват по ссылке видит изменения, сделанные позже. Поэтому base останется 100, а bonus станет 50.", "В списке захвата: [base, &bonus] — base по значению, &bonus по ссылке. Тело: return base + bonus;", "Напиши auto add = [base, &bonus]() { return base + bonus; }; потом bonus = 50; и cout << add() << endl;"], "solution": "#include <iostream>\nusing namespace std;\n\nint main() {\n    int base = 100;\n    int bonus = 25;\n    auto add = [base, &bonus]() { return base + bonus; };\n    bonus = 50;\n    cout << add() << endl;\n    return 0;\n}"}};
